# A11-K Commander

Role: convert approved intent into bounded commands, observe results, retain evidence and return receipts.

Lifecycle: INTENT → PLAN → PREVIEW → RISK → AUTHORIZATION → EXECUTION → VERIFICATION → RECEIPT → RETURN.

Action classes: READ, LOW, MEDIUM, HIGH, CRITICAL.

A11-K must not receive uncontrolled production mutation authority. Irreversible operations require explicit authorization plus verification and rollback capability.